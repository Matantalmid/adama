import { ComingSoon } from "@/components/layout/ComingSoon";

export const metadata = { title: "נכס חדש · אדמה" };

export default function NewPropertyPage() {
  return (
    <ComingSoon
      title="הוספת נכס / דיל"
      description="כתובת, מחיר, תקציב שיפוץ ו-ARV ראשוני — ומיד בדיקת כלל ה-70%."
    />
  );
}
